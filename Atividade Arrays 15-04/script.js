// Escreva um script que remova o ultimo elemento de um array:

const person = ["Felipe", 30, "São Paulo", "Masculino", "584651671658"];

person.pop();

console.log(person);

// Escreva um script que adiciona um novo elemento no inicio do array:

const jobs = ["Frontend", "Backend", "Devops", "FullStack"];

jobs.unshift("Design");

console.log(jobs);

// Escreva um script que adiciona um elemento no final do array:

const users = ["Ana", "Fernando", "Rafael", "Clara", "Maria"];

users.push("Roger");

console.log(users);
